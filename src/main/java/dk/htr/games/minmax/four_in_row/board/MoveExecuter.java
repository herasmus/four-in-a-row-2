package dk.htr.games.minmax.four_in_row.board;

public class MoveExecuter {
    /*
    long move(long boardBefore, GameDimensions dimensions) throws GameException {
        if(dimensions.nrOfRows() == 4) {
            return move4RowBoard(boardBefore, dimensions);
//        } else if(numberOfRows == 6) {
//            return sixRows[column];
        } else {
            throw new GameException("Doesn't handle nr of rows other than 4");
        }
    }*/
/*
    long move4RowBoard(long boardBefore, int move, GameDimensions dimensions) throws GameException {
        if(BoardState.isBoardValid(boardBefore, dimensions)) throw new BoardStateException()



        if(!ValidColumnStateChecker.isValid4RowColumn())
        return -1;
    }*/
}
